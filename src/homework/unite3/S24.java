package homework.unite3;
import java.util.Random;

// (Game: pick a card)
// Write a program that simulates picking a card from a deck of 52 cards.
// Your program should display the rank (Ace, 2, 3, 4, 5, 6, 7, 8, 9, 10, Jack, Queen, King)
// and suit (Clubs, Diamonds, Hearts, Spades) of the card.
// Here is a sample run of the program:
// The card you picked is Jack of Hearts
public class S24 {
    public static void main(String[] args) {
        // switch case ile yazılabilir
        // array ile yazılabilir
        // bide üst düzeyde maping kullanarak
        /*
        Random card = new Random();
        String [] rank  = {"Ace", "2", "3", "4", "5", "6", "7", "8", "9", "10", "Jack", "Queen", "King"};
        String [] suit = {"Clubs", "Diamonds", "Hearts", "Spades"};

        int rankIndex = card.nextInt(rank.length);
        int suitIndex = card.nextInt(suit.length);

        String pickedCard = rank[rankIndex] + " of " + suit[suitIndex];

        System.out.println("The card you picked is " + pickedCard);
        */
    }
}